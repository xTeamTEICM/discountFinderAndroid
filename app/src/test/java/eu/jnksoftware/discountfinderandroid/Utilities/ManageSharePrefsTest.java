package eu.jnksoftware.discountfinderandroid.Utilities;

import android.content.Context;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mockito;

import eu.jnksoftware.discountfinderandroid.models.token.User;

import static org.junit.Assert.*;

/**
 * Created by makis on 13/1/2018.
 */
public class ManageSharePrefsTest {

  /*  final Context context = Mockito.mock(Context.class);
    @Before
    public void setUp() throws Exception {
        ManageSharePrefs.init(context);
    }
    @Test
    public void readUser() throws Exception {
        User user=new User("token_type",7000,"access_token","refresh_token");
        ManageSharePrefs.writeUser(user);
        User readedUSer=ManageSharePrefs.readUser(null);
        Assert.assertEquals(user.getAccessToken(),readedUSer.getAccessToken());

    }

    @Test
    public void writeUser() throws Exception {
    }*/

}